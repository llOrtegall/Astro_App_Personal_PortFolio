---
interface Props {
  title: string;
  company: string;
  description: string;
  date: string;
  tecnologias: string[];
}
const { title, company, description, date, tecnologias } = Astro.props;
---

<div class="flex flex-col gap-4 pt-2">
  <time class="">💻 {date}</time>
  <h2 class="text-sm font-semibold text-gray-200">
    Compañia: {company}
  </h2>
  <h3 class="text-sm font-semibold text-blue-500 text-pretty">
    {title}
  </h3>
  <p class="text-sm text-wrap font-normal text-gray-500">
    {description}
  </p>

  <ul>
    <li>
      <span class="text-gray-200 text-pretty">Tecnologías Implementadas: </span>
      <article class="grid grid-cols-3 text-center">
        {
          tecnologias.map((tecnologia) => (
            <span class="text-yellow-400">{tecnologia}</span>
          ))
        }
      </article>
    </li>
  </ul>
</div>
