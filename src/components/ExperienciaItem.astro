---
interface Props {
  title: string;
  company: string;
  description: string;
  date: string;
  tecnologias: string[];
}
const { title, company, description, date, tecnologias } = Astro.props;
---

<div
  class="absolute w-3 h-3 bg-gray-200 rounded-full mt-0 -start-1.5 border border-white dark:border-gray-900 dark:bg-gray-700"
>
</div>
<time class="">💻 {date}</time>
<h2 class="text-2xl font-semibold text-gray-200">
  Compañia: {company}
</h2>
<h3 class="text-sm font-semibold text-blue-500 text-pretty py-2">
  {title}
</h3>
<p class="text-lg text-wrap font-normal text-gray-200">
  {description}
</p>

<ul class="py-4">
  <span class="text-gray-200 text-pretty">Tecnologías Implementadas: </span>
  <article class="flex gap-4 pt-2">
    {
      tecnologias.map((tecnologia) => (
        <span class="text-yellow-400">{tecnologia}</span>
      ))
    }
  </article>
</ul>
