---
interface Props {
  title: string;
  company: string;
  description: string;
  date: string;
  tecnologias: string[];
}
const { title, company, description, date, tecnologias } = Astro.props;
---
<div class="absolute w-3 h-3 bg-gray-200 rounded-full mt-0 -start-1.5 border border-white dark:border-gray-900 dark:bg-gray-700">
</div>
<time class="">💻 {date}</time>
<h2 class="text-sm font-semibold text-gray-200">
  Compañia: {company}
</h2>
<h3 class="text-sm font-semibold text-blue-500 text-pretty">
  {title}
</h3>
<p class="text-sm text-wrap font-normal text-gray-200">
  {description}
</p>

<ul>
  <li>
    <span class="text-gray-200 text-pretty">Tecnologías Implementadas: </span>
    <article class="grid grid-cols-4 p-2 gap-2">
      {
        tecnologias.map((tecnologia) => (
          <span class="text-yellow-400">{tecnologia}</span>
        ))
      }
    </article>
  </li>
</ul>
